<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gzhang.SpotifyProject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainNavigationActivity"
            android:label="@string/title_activity_main_navigation"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LoginAndConnectActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <service
            android:name=".OnCloseAppService"
            android:stopWithTask="false" />

        <activity
            android:name=".PlaySongActivity"
            android:label="@string/title_activity_play_song"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MenuInflaterActivity" />
        <activity
            android:name=".SearchSongActivity"
            android:label="@string/title_activity_search_song"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>